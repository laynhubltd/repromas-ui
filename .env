# API base URL (optional; defaults to http://localhost:8000/api)
# VITE_API_BASE_URL=http://localhost:8000/api

# Use mock login so you can access the dashboard without the real API (dev only)
VITE_USE_MOCK_AUTH=false